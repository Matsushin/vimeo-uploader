.row
  .col-md-8.col-md-offset-2
    = link_to new_movie_path, class: 'btn btn-default' do
      = fa_icon 'plus'
      動画を追加する

    %h1 動画一覧
    - @movies.each do |movie|
      %p= movie.title
      %p= movie.url